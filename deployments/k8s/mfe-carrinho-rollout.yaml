apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: mfe-carrinho
  namespace: fiap-store
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mfe-carrinho
  template:
    metadata:
      labels:
        app: mfe-carrinho
    spec:
      containers:
        - name: mfe-carrinho
          image: ghcr.io/gerps2/fiap-store-mfe-carrinho:latest
          ports:
            - containerPort: 80
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: {}
        - setWeight: 30
        - pause: { duration: 60 }
        - setWeight: 60
        - pause: { duration: 60 }
---
apiVersion: v1
kind: Service
metadata:
  name: mfe-carrinho
  namespace: fiap-store
spec:
  selector:
    app: mfe-carrinho
  ports:
    - port: 80
      targetPort: 80
